spring:
  application:
    name: bibliotheque
  datasource:
    url: jdbc:h2:mem:biblio;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  thymeleaf:
    cache: false

server:
  port: 9090

logging:
  level:
    root: INFO
    cours.biblio: DEBUG

